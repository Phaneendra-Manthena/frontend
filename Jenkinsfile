@Library('roboshop') _
pipeline{
 agent any
   stages {
     stage('TEST') {
     steps {
     script{
        def abc = "Hello"
        def xyz = 10

        print "abc = ${abc}"
        print abc

        }
       }
     }
   }
}